using UnityEngine;
using System.Collections;

public class flamingo : MonoBehaviour {

	// Use this for initialization
	void Start () {
        transform.Translate(new Vector3(0, 1.15f, 0));
    }
	
	// Update is called once per frame
	void Update () {
	
	}
}
