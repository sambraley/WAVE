using UnityEngine;
using System.Collections;

public class hedge : MonoBehaviour {

	// Use this for initialization
	void Start () {
        transform.Rotate(new Vector3(-90, 0, 0)); //b/c hedges r dumb
    }
	
	// Update is called once per frame
	void Update () {
	
	}
}
